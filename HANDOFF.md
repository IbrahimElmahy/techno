# تسليم مشروع techno — كل اللي اتعمل واللي لسه ناقص

> الملف ده مكتوب عشان نموذج ذكاء اصطناعي تاني يقراه ويكمّل من غير ما يسأل من الأول.
> آخر تحديث: ٢٠٢٦-٠٨-٣٠.
> اقرا `PROMPT.md` كمان — بس **صحّح منه**: النشر مش أوتوماتيكي من GitHub زي ما مكتوب هناك.

---

## ١. إيه المشروع ده

`D:\techno` — نظام ERP بالعربي المصري لشركة **تكنو ثيرم** (سخانات). الهدف: يحلّ محل نظامهم
القديم اللي اسمه **a5 system**.

| | |
|---|---|
| `backend/` | FastAPI + SQLAlchemy 2 + Pydantic v2. بايثون 3.11 في `backend/.venv` |
| `frontend/` | React 18 + antd v5 + TypeScript + Vite. عربي RTL. **HashRouter** |
| `mobile/` | **Flutter** (مش React Native) — تطبيق المعاينات للمناديب، offline بـsqflite |
| قاعدة البيانات | محلي: MySQL. السيرفر: **PostgreSQL 16** |

النظام **كله للاختبار لسه** — مافيش بردكشن حقيقي شغّال عليه.

### نظامين قديمين بيتنقل منهم

| القاعدة | السيرفر | فيها إيه |
|---|---|---|
| `aliaa2026` | SQL Server على `A5SERVER` | فرع **العلياء** — مبيعات، مخازن، حسابات |
| `Techno2026` | نفس السيرفر | فرع **أكتوبر** — نفس الحاجة |
| `ERP` | نفس السيرفر | **ما بعد البيع**: المعاينات، الكوبونات، الفنيين، التجار |

النظام الحيّ بتاعهم: <https://a5system-eg.com/>

---

## ٢. النشر — مهم جداً، وفيه غلطة شائعة

**الفرونت والباك اند بيتقدّموا من سيرفر ويندوز عند العميل، مش من السحابة.**

```
local.technothermeg.com  ──(Cloudflare Tunnel)──►  127.0.0.1:8000  (خدمة NSSM: TechnoApi)
                                                    │
                                                    ├── API على /api/v1/*
                                                    └── الواجهة من C:\techno\frontend\dist
```

- ✅ **`https://local.technothermeg.com`** ← ده الشغّال. ده اللي تنشر عليه وتجرّب عليه.
- ❌ `https://app.technothermeg.com` ← نشر Vercel **قديم وميّت**. الباك إند اللي
  فيه (`techno-flax-alpha.vercel.app`) راجع 404، فتسجيل الدخول عليه بيفشل بـCORS.
- ❌ `https://api.technothermeg.com` ← برضه من السحابة القديمة.

### خطوات النشر

```bash
# ١) الفرونت
cd D:/techno/frontend && npx tsc --noEmit -p tsconfig.json && npx vite build
scp -q -r dist/assets dist/index.html Administrator@ssh.technothermeg.com:C:/techno/frontend/dist/

# ٢) الباك إند
scp -q backend/src/api/<file>.py Administrator@ssh.technothermeg.com:C:/techno/backend/src/api/
# وبعدين إعادة تشغيل الخدمة (شوف rps.sh تحت)
```

> **متستخدمش `tar` على السيرفر** — ويندوز 2012 R2 مافيهوش. استخدم `scp -r`.

### الدخول على السيرفر

`ssh Administrator@ssh.technothermeg.com` — بس جلسة الـSSH بتقع في `cmd.exe`، فأي `|` أو
`"` بيتفسّر مرتين. فيه سكربتات مساعدة في مجلد الـscratchpad:

| السكربت | بيعمل إيه |
|---|---|
| `rps.sh '<powershell>'` | يشغّل PowerShell على السيرفر بـ`-EncodedCommand` base64/UTF-16LE — بيحلّ الاقتباس والعربي |
| `run.sh` | يشغّل موديول بايثون على السيرفر ويرجّع الخرج بترميز صح (بيكتب لملف وبيـscp‑ـه) |
| `psql.sh` / `psqlf.sh` | يشغّل SQL على Postgres بتاع السيرفر (بيقرا `DATABASE_URL` من `.env`) |

مثال:
```bash
bash "$SP/rps.sh" 'Restart-Service TechnoApi; Start-Sleep -Seconds 25; (Invoke-WebRequest "http://127.0.0.1:8000/health" -UseBasicParsing).StatusCode'
```

---

## ٢٫٥ 🚫 قواعد النظامين القدام — قراءة فقط، مافيش استثناء

`aliaa2026` و`Techno2026` (a5) و`ERP` على SQL Server = **`SELECT` والتصدير بس**.

ممنوع `INSERT` · `UPDATE` · `DELETE` · `ALTER` · `DROP` · `CREATE` — **ولا حتى جدول مؤقت
أو تصحيح «بسيط»**.

كل الكتابة على **Postgres بتاعنا** على السيرفر (`DATABASE_URL` في `backend/.env`).

**ليه؟** سببين وأي واحد فيهم يكفي:
1. النظامين لسه شغّالين عند العميل دلوقتي، ومافيش نسخة احتياطية تحت إيدنا نرجّع منها.
2. هما مرجع الحقيقة اللي بنقارن بيه صحة النقل — مصدر إحنا عدّلناه مابيثبتش حاجة.

محتاج تصلّح داتا منقولة؟ صلّحها في Postgres بتاعنا بسكربت في `backend/src/scripts/`،
مش في المصدر.

قبل أي `sqlcmd ... -d ERP` أو `-d aliaa2026` أو `-d Techno2026`: **اقرا الاستعلام وتأكد
إنه `SELECT`.**

---

## ٣. قواعد شغل المستخدم — اتقالت صراحةً، اتبعها

1. **متشغّلش سويتات اختبار.** لا `pytest` ولا `vitest`. الاختبارات اتمسحت بطلبه.
   الكفاية: `npx tsc --noEmit` للفرونت، `python -c "import src.main"` للباك إند،
   `dart analyze lib/` للموبايل.
2. **متكتبش اختبارات جديدة** — موديل تاني بيعمل دي.
3. **الكلام قليل جداً.** النتيجة الأول. المستخدم زهق من الردود الطويلة وقال كده أكتر من مرة.
4. **متاخدش وقت طويل في التاسك الواحد.** قال ده صراحةً. متلفّش كتير في الاستكشاف.
5. **الرفع لما يطلب بس.** `git commit` محلي، و`git push` لما يقول «ارفع».
   الرفع بحساب **`IbrahimElmahy`** في `gh` (أي حساب تاني بياخد 403).
6. **عمود جديد في القاعدة؟** لازم يتسجّل في `backend/src/main.py` — **مافيش Alembic**:
   - `_ADDED_COLUMNS` — عمود جديد
   - `_WIDENED_COLUMNS` — VARCHAR/ENUM اتوسّع
   - `_NULLABLE_FK_COLUMNS` — عمود بقى اختياري
   - `_DROPPED_CONSTRAINTS` / `_ADDED_CONSTRAINTS`
7. **مافيش باركود** — الميزة اتشالت بطلب العميل. متضفهاش تاني حتى لو a5 عندها.
8. **أصناف المعاينة مابتخصمش من عهدة المندوب** — `item_id` بيفضل `null`. نقاط بس.

---

## ٤. اللي خلص في الجلسة دي

### ٤.١ نقل الداتا من a5

سكربتات النقل كلها في `backend/src/scripts/`. **كلها idempotent، ودرايَ‑رن افتراضياً،
و`--yes` بتنفّذ**:

| السكربت | بينقل إيه |
|---|---|
| `import_a5.py` | البيانات الأساسية (أصناف، عملاء، موردين، مخازن) — `--branch` / `--prefix` |
| `import_a5_phase2.py` | شجرة الحسابات، ربط المندوب بالمخزن، الأرصدة الافتتاحية |
| `import_a5_emp.py` | الموظفين — `_same_person()` بيطابق بالكلمات |
| `import_a5_docs.py` | فواتير البيع/الشراء/المردود/التحويلات/الأذونات |
| `import_a5_ledger.py` | القيود عن طريق `sysfree`؛ بيربط المستندات بـ`ord`/`OrdBk`/`poord`/`PoordBk` |
| `seed_branches_a5.py` | الفروع |
| `merge_a5_customers.py` | دمج العملاء المكررين |
| `set_a5_families.py` / `fix_customer_types.py` / `classify_a5_parties.py` | التصنيفات |
| `sync_a5_lookups.py` | القوايم المنسدلة |
| `link_a5_party_accounts.py` | ربط الأطراف بحساباتهم في الشجرة |
| `import_erp_parties.py` | أطراف نظام ما بعد البيع (عملاء/سباكين/موزعين/مناديب خدمة) |
| `import_erp_visits.py` | المعاينات وبنودها وأصنافها |
| `import_erp_coupons.py` | صرف واستلام الكوبونات |

**التحقق من صحة النقل** (اتعمل واتأكد):
- رصيد كل صنف مطابق لـa5 بالظبط: ٥٦٢/٥٦٢ أكتوبر، ٢٠٧٨/٢٠٧٨ العلياء.
  المجاميع ١١٨٬٠٧٧ و١٥١٬٦٧٧.
- ٦١٦ رصيد سالب (صنف×مخزن) — **دي حقيقة a5 نفسها** (٣٣٩ + ٢٧٧)، مش غلطة نقل.
- تدقيق ١٨ قاعدة عمل: ١٣ نضيفة. اتصلّح ٧١٥٠ فاتورة مش مربوطة (عمود ربط غلط) لحد ٦٠٩
  (فواتير بونص a5 نفسها مالهاش قيد).

### ٤.٢ الأداء

المشكلة كانت حقيقية مش مؤقتة (`/sales` كان بياخد ٤٦٫٩ ثانية والعميل بيقطع عند ٣٠).

- شيل N+1: بحث مجمّع بدل استعلام لكل صف، و`selectinload`
- `chart_service.bulk_balances(db)` — استعلامين للشجرة كلها بدل واحد لكل حساب
- `contact_service.bulk_phone_values(db, owner_type, owner_ids)`
- `GZipMiddleware`
- كاش: `no-store` على `index.html`، `immutable` على الأصول اللي ليها بصمة
- `limit`/`offset` على قوايم المبيعات + endpoint جديد `/sales/summary`

**النتيجة: ١٢١ث → ١٢٫٢ث عبر النفق، و٢٦٫٤ث → ٦٫٤ث على السيرفر نفسه.**

### ٤.٣ الكوبونات — إعادة تصميم

**المشكلة:** الترقيم ١‑٥٠ ذهبي و١‑٥٠ فضي كانوا بيتخبطوا. النظام كان شايف الرقم بس.

**الحل:** هوية الكوبون بقت **(الفئة + الرقم)**. مطابق لـa5 نفسها اللي بتكتب
`NumCoupon = "ذهبى-536000"`.

**موديل جديد:** `backend/src/models/coupon_issue.py` — مستند «صرف كوبونات» اللي كان ناقص.
النظام كان يعرف طريق واحد بس للكوبون يخرج بيه (سطر على فاتورة بيع)، وده نص الحكاية:
الشركة بتصرف دفاتر لموزعين وتجار من غير بيع.

```
CouponIssue      → document_number, branch_id, customer_id, coupon_kind, issue_date,
                   count, unit_value, rep_user_id, notes, external_ref, actor_user_id
CouponIssueLine  → UniqueConstraint("coupon_kind", "serial")
```

**خدمات:** `coupon_receipt_service.find_issuing_invoice()` / `find_issuing_issue()` /
`already_received()` / `kinds_for_serial()` / `check_serial()` (بيرجّع
`coupon_kind`/`kinds`/`ambiguous`).

**اتشال:** كتالوج استبدال النقاط — المستخدم قال «ملهاش أي أساس عندي ولا حتى في النظام
القديم بتاع ما بعد البيع».

### ٤.٣٥ دورة الكوبون — المنطق اللي كل حاجة مبنية عليه

المستخدم شرحها بالنص:

> «أنا عندي نقاط على المنتجات في البيع، والنقاط دي بسلّمها للتجار على هيئة كوبونات،
> والتاجر بيدّي الكوبونات دي للفني كنوع من التسويق، وأنا بستلم الكوبونات من الفنيين
> أو من التجار ساعات.»

```
منتج عليه نقاط  ──►  التاجر  ──►  الفني (السباك)  ──►  يرجع لينا
   (بيع)          (كوبونات)      (تسويق)         (من الفني أو من التاجر)
```

**النتيجة المباشرة: التاجر في ما بعد البيع هو نفسه تاجر البيع — دايماً.**

عشان الكوبون أصلاً جاي من نقاط بيع اتعملت **له**. مافيش تاجر في `wh_Merchants`
مالوش وجود في نظام البيع؛ الفكرة نفسها مستحيلة.

**فلو سكربت مطابقة طلّع تاجر «مالوش مقابل» — دي غلطة مطابقة، مش طرف جديد.**
غالباً فرق إملائي في الاسم (لقب، «عبد» و«عبدالـ»، مسافات، «تكنو» زايدة أو ناقصة).

⛔ **ممنوع تعمل عميل جديد للتاجر اللي مااتطابقش.** ده بيعمل نسخة تانية لنفس الراجل —
بالظبط المشكلة اللي الدمج اتعمل عشانها. اطبع قايمتهم للمراجعة اليدوية وخلاص.

نفس المنطق بيفسّر تصميم «تقارير المتابعة»: الموزع/التاجر ماسك ورق ولازم يرجّعه،
والفني بيجيب ورق — فتبويبين مش واحد.

### ٤.٤ حقول ومفاهيم جديدة

- `Customer.service_rep_id` — **مندوب خدمة العملاء**، غير مندوب المبيعات.
  اتعمل ١٦ مندوب خدمة كمستخدمين `svc.{erp_id}`، واتربط بيهم ٩٢٦٢ عميل.
- تصنيف العملاء: **تاجر / سباك / مالك** — دول التلاتة بس.
  - التاجر: بيع وشراء
  - السباك: بيرجّع الكوبونات (وممكن التاجر يرجّع كمان)
  - المالك: صاحب البيت اللي بتتعمل له المعاينة
  - ⚠️ كنت اخترعت «معرض/شركة/مؤسسة» — **دول مالهمش وجود في a5 خالص واتشالوا**.
- شاشة الفواتير: عمودين منفصلين — «نوع الفاتورة» (العيلة: أبيض/بولي) و«النوع» (تصنيف العميل).

### ٤.٥ تصحيح مناديب المعاينات

كانت الـ١٠٬٩٢٢ معاينة كلها على مندوب واحد. المصدر `SalesRepId` على الزيارة.
**اتصلّح ١٠٬٨١٨ معاينة** — `/inspections/by-rep` بقى بيرجّع ١٢ مندوب حقيقي:

| المندوب | معاينات |
|---|---|
| احمد تركى | ٣٬٠٤٦ |
| اشرف هلول | ٢٬٦٢٢ |
| حسن عيد | ١٬٦٣٨ |
| بيومى جابر | ١٬٢٤٤ |

### ٤.٦ شاشة «تقارير المتابعة» (آخر حاجة خلصت)

- `backend/src/api/after_sales_reports.py` — ٥ نقاط تحت `/api/v1/after-sales-reports`
- `frontend/src/pages/AfterSalesReports.tsx` — ٤ تبويبات
- القايمة: **خدمات ما بعد البيع ← تقارير المتابعة**

| التبويب | بيقول إيه | صفوف |
|---|---|---|
| كوبونات الموزعين | اتصرف له / رجع / لسه برّه | ٥٩ |
| كوبونات السباكين | رجّع كام | ٤٣٧ |
| الزيارات بنقاط الفني | معاينات + نقاط | ٩٠٤ |
| زيارات المناديب | معاينات + عملاء + نقاط | ١٢ |

**قرار تصميم مهم:** السباك والموزع في **تبويبين مش واحد**. الورقة بتتصرف للموزع وبترجع من
السباك، فعمود «لسه برّه» على السباك بيطلع بالسالب على ورق مااتصرفش له أصلاً.

**باج اتصلّح فيها:** `_returned_against_issue()` كانت بتـjoin على `CouponIssueLine` قبل
`CouponReceiptLine` — fan‑out بيضرب العدد في عدد أوراق المستند. «لسه برّه» كانت
**‑٤٧٬٦٣٢**، بقت **٥١٧**. الربط بقى على المستند مباشرةً.

الكوميت: `4e4c8da`

---

## ٥. اللي لسه ناقص — ابدأ من هنا

### ٥.١ 🔴 شاشة المعاينات — تلات حاجات (التاسك الحالي، لسه ماخلصش)

المستخدم بعت سكرين شوت لشاشة «مراجعة زيارات المناديب (المعاينات)» وقال:
> «الصفحه دي ناقصها التاجر مش شغال و كمان نوع المعاينه و الحاله بتاعتها فيها غلطات»

**البحث خلص وعرفنا المصدر بالظبط.** جدول `Wh_TechnicalVisits` في قاعدة `ERP` فيه أعمدة
النقل ماخدهاش:

| عندنا | مصدره في `Wh_TechnicalVisits` | حالته |
|---|---|---|
| **التاجر** — لازم يبقى **رابط لعميل** مش نص | `MerchantsID` → `wh_Merchants` | ❌ مانُقلش خالص |
| `visit_type` (**نوع الزيارة**) | `IsMarma` bit: `1` = مرمة (٣٬٧٧٥) · `0` = معاينة (٧٬٠٣٩) · NULL (١١٣) | ❌ كله دخل «معاينة» |
| `printed` (**الطباعة**) | `CountPrint` int (٢٬٠٥٦ اتطبعوا مرة، و١٢٥ أكتر) | ❌ كله «غير مطبوعة» |
| `status` (**الحالة**) | **مافيش عمود قبول/رفض عندهم** | ⚠️ اقرا تحت |

> 🔴 **تصحيح من المستخدم (مهم):** «التاجر هو نفس التاجر اللي عندي في البيع. هما كانوا
> شغالين على نظامين منفصلين للبيع وما بعد البيع، وإحنا دمجناهم، فالمفروض التاجر واحد.»
>
> يعني **متحطّهوش نص في `purchase_shop`**. لازم يبقى **مفتاح خارجي على `customer.id`**
> زي `customer_id` و`service_rep_id` بالظبط — عشان كارت التاجر الواحد يجمع بيعه ومعايناته
> وكوبوناته في مكان واحد.
>
> تجار الـERP **متنقولين خلاص** كعملاء بكود `ERP-M-{id}` — **١١٤ تاجر**.
> (المجموع في القاعدة: `ERP-C` ٧١٨٥ · `ERP-P` ٢٠٥٦ · `ERP-D` ٧٥ · `ERP-M` ١١٤)
>
> **لازم تتأكد الأول:** هل الـ١١٤ دول اتدمجوا مع تجار a5 ولا لسه نسخ منفصلة؟
> لو لسه منفصلين، الدمج لازم يحصل **قبل** ما تربط المعاينات بيهم — وإلا هتربط على
> النسخة الغلط. شوف `merge_a5_customers.py`.

`PurchaseStore` (النص الحر) متطابق مع `m.Name_Ar` في ١٠٬٦٠٢ صف ومختلف في ١٤٤ — يبقى نص
احتياطي بس، مش المصدر.

**بخصوص «الحالة»:** `InspectionStatus` (مقبولة/مرفوضة) **اختراعنا إحنا**، مش من نظامهم.
أقرب حاجة في قاعدتهم هي `OutRange` bit (٥٩٢ زيارة اتسجّلت **برّه النطاق الجغرافي**
المسموح) — ودي إشارة مراجعة حقيقية بس مش قبول/رفض. **لازم تسأل المستخدم** «الحالة»
عايزها تيجي منين قبل ما تخترع حاجة تانية — الغلطة دي اتعملت قبل كده مع تصنيف العملاء
والمستخدم رفضها.

**الأعمدة اللي اتفحصت واتستبعدت:** `Code` (٢٦٢١ قيمة مميزة على ١٠٬٩٢٧ صف — مش رقم شهادة)،
`ResponseCode` (٠/١/١٩٠٣ — كود رد المزامنة)، `VisitType` bit (٢٨٨ صفر مقابل ١٠٬٦٣٩ واحد).

**الخطة الجاهزة للتنفيذ:**

1. صدّر من SQL Server (الملف ده كان جاهز واتوقف قبل ما يشتغل):
```sql
SET NOCOUNT ON;
SELECT CAST(v.ID AS VARCHAR) + '~'
  + REPLACE(REPLACE(ISNULL(NULLIF(LTRIM(RTRIM(v.PurchaseStore)),''), ISNULL(m.Name_Ar,'')),
            CHAR(13),' '),CHAR(10),' ') + '~'
  + REPLACE(REPLACE(ISNULL(m.Mobile1, ISNULL(m.Phone,'')),CHAR(13),' '),CHAR(10),' ') + '~'
  + CASE WHEN ISNULL(v.IsMarma,0) = 1 THEN N'مرمة' ELSE N'معاينة' END + '~'
  + CAST(ISNULL(v.OutRange,0) AS VARCHAR) + '~'
  + CAST(ISNULL(v.CountPrint,0) AS VARCHAR)
FROM Wh_TechnicalVisits v LEFT JOIN wh_Merchants m ON m.ID = v.MerchantsID;
```
2. ضيف عمود `Inspection.merchant_customer_id` (FK على `customer.id`، nullable)
   وسجّله في `_ADDED_COLUMNS` في `main.py`.
3. سكربت patch بيلاقي المعاينة بـ`document_number = "ERP-V-{ID}"` ويعبّي
   `merchant_customer_id` (بمطابقة `ERP-M-{MerchantsID}` على كود العميل) +
   `visit_type` + `printed`. و`purchase_shop` يفضل النص الاحتياطي.
4. حدّث `exp_visits.sql` و`import_erp_visits.py` كمان عشان أي نقل جاي ياخدهم.
5. ضيف «مرمة» لقايمة `visit_type` في `LookupOption`.
6. في الفرونت `pages/Inspections.tsx`: عمود «التاجر» يبقى لينك بيفتح كارت العميل
   (زي عمود العميل في شاشة الفواتير)، مش نص.

### ٥.٠٥ 🟢 أمر شغل ٠٠٧ — ترقيم الصفحات وملخص السيرفر (تم تنفيذه بالكامل)

**المشكلة قبل الحل:**
- `/inspections` كانت بتنزّل ١٩٫٤ ميجابايت في ٢٤٫٨ ثانية.
- `/coupon-receipts` و`/customers` بينزلوا الجداول كاملة.
- كروت الإحصائيات في رأس الصفحات كانت بتحسب على الداتا المحملة محلياً.

**الحل المنفذ:**
1. **عقد ترقيم الصفحات الموحد (`GET /<resource>?limit=...&offset=...`)**:
   - لما يتبعت `limit`: يرجع `{"rows": [...], "total": count, "limit": limit, "offset": offset}` مع هيدر `X-Total-Count`.
   - لما ما يتبعتش `limit` (تطبيق الموبايل Flutter `pullReferenceData`): يرجع `list` مباشر مع هيدر `X-Total-Count` لضمان التوافق الكامل دون أي كسر.
2. **نقاط النهاية للملخصات السريعة (`/summary`)**:
   - `GET /inspections/summary`
   - `GET /coupon-receipts/summary`
   - `GET /customers/summary`
   تعتمد على استعلامات SQL تجميعية (`func.count`, `func.sum`, `func.coalesce`) وتستجيب في < 100ms.
3. **النتائج المقاسة على السيرفر بعد التطبيق:**
   - `/inspections?limit=50`: الحجم انخفض من **١٩ ميجا** إلى **٩١ كيلوبايت** (انخفاض ٩٩٫٥٪)، والزمن انخفض من **٢٤٫٨ ثانية** إلى **١٫٨ ثانية**.
   - `/coupon-receipts?limit=20`: الحجم انخفض من **١٫٧ ميجا** إلى **١٧٫٧ كيلوبايت**، والزمن من **٢٫٦ ثانية** إلى **٠٫٧ ثانية**.
   - `/customers?limit=20`: الحجم انخفض من **١٫٧ ميجا** إلى **٨٫٣ كيلوبايت**، والزمن انخفض إلى **٦٨ ملي ثانية**.
   - ملخصات المعاينات والكوبونات والعملاء تنفذ في **٤٤–١٠٩ ملي ثانية**.

### ٥.١٥ 🟡 رصيد النقاط الافتتاحي للتجار — مؤجّل بقرار المستخدم

**القرار (٢٠٢٦-٠٨-٣٠):** «نبدأ برصيد افتتاحي، بس مش هنعمله دلوقتي — جهّز الصفحات الأول.»

**ليه أصلاً:** نظام النقاط عندنا بيبدأ من صفر، والكسب الرجعي اتحسب على الفواتير
المنقولة — وهي **٢٠٢٦ بس** (٨٬٠١٤ فاتورة من يناير لأغسطس). بينما المعاينات من **٢٠١٠**
(١٠٬٩٢٢ معاينة). فالتاجر بيكسب من سنة واحدة وشغله القديم مش بيبان.

المستخدم قرر: **الكسب الرجعي بس** (٤٦٦٬٩٧٦ نقطة على ٢٨١ عميل)، والخصم يبدأ من
لحظة تشغيل النظام. الفرق بين ده وبين رصيده الحقيقي بيتظبط برصيد افتتاحي.

**اللي محتاجه لما ييجي وقته:** مافيش تغيير في الموديل —
`PointKind.adjustment` موجود خلاص. المطلوب شاشة إدخال بس: تاجر + مبلغ + سبب،
بتكتب سطر `adjustment` واحد. ومهم يبقى فيه سبب مكتوب على السطر، لأن الرصيد
الافتتاحي رقم مالوش مستند يرجع له.

⛔ **متحسبوش ومتستنتجوش.** الرقم بيتدخّل من المستخدم — إحنا مانعرفش رصيد التاجر
الحقيقي، ومحدش كان بيحسب النقاط في النظام القديم أصلاً.

### ٥.٢ 🟡 مقارنة تقارير a5

استُخرج من `A5Group_Acc.exe` إن عندهم **~٤٥ تقرير** موزّعين على أقسام. عندنا ٦ شاشات
تقارير بس. المستخدم طلب: «عايزها بنفس طريقة العمل والعرض».

المطلوب: مقارنة القايمتين وعمل الناقص.

### ٥.٣ 🟡 حاجات صغيرة معلّقة

- **٢١ طرف** لسه من غير حساب مربوط في الشجرة (١٩ اسم ملتبس + الشركة الشقيقة)
- **٤ أزواج دمج عملاء** اتخطّوا (الاسم المجرّد ظاهر مرتين — محتاج قرار من المستخدم)
- **الوصول من الشبكة المحلية** — ربط الـAPI على `0.0.0.0` بدل `127.0.0.1`.
  اتعرض على المستخدم ومحتاج موافقته الصريحة لأنه بيوسّع التعرّض الشبكي.
- 🔐 **تغيير باسورد `Administrator` بتاع السيرفر**، وتغيير `admin`/`admin123`

---

## ٦. مصايد اتوقعنا فيها — متكررهاش

### SQL Server / sqlcmd

- **ملف SQL فيه عربي `LIKE N'%كذا%'` متكتبوش UTF-8** — sqlcmd بيقراه غلط والنتيجة صفر
  صفوف من غير أي رسالة خطأ. اعمل الفلترة في بايثون، أو اكتب الملف بـUTF-16LE.
- التصدير: UTF-16LE بفاصل `~`. **لازم تشيل CR/LF من الأعمدة النصية**
  (`REPLACE(...,CHAR(13),' '),CHAR(10),' ')`).
- `ISNULL(bit_col, -1)` **مابيميّزش NULL** — الـ‑١ بيتحوّل لـ`1`. استخدم
  `CASE WHEN x IS NULL THEN 'NULL' ELSE CAST(x AS VARCHAR) END`.
- PowerShell 2 على السيرفر (ويندوز 2012 R2): **مافيش `-Depth`** في `Get-ChildItem`.

### بايثون / SQLAlchemy

- **دالتين بنفس الاسم في نفس الملف** = بايثون بياخد الأخيرة في صمت. حصلت مرتين:
  `_relax_not_null` و`_sync_constraints`. **دوّر على الاسم قبل ما تعرّف دالة جديدة.**
- `except:` فاضية بتبلع الاستثناء — قيد قديم فضل موجود من غير ما حد يعرف.
  حط `log.warning` على الأقل.
- `AccountType.chart` و`AccountNature.revenue` **مش موجودين** — الصح `user_defined`
  و`income`.
- سكربت باتش بيعمل `assert` وبعدين `write`: **assert فاشل في الآخر بيلغي كل التعديلات
  اللي نجحت قبله**. اكتب بعد كل استبدال، أو اعمل الـasserts كلها الأول.
- **متسمّيش ملف مؤقت `inspect.py`** — بيحجب مكتبة بايثون الأساسية.
- `r"^[\x00-\x7f]+$"` مكتوبة جوّه سلسلة بايثون بتكتب **بايت NUL حقيقي** في ملف الـ`.py`.
  البديل: `re.compile(r"^[^\u0600-\u06ff]+$")`.
- **اقص كل نص على حد عموده** قبل الإدخال. `floor_number` عندنا `VARCHAR(16)` و«الدور
  الثاني شقه 2» بيوقّف النقل كله.
- متنادش دالة بحث مجمّع **جوّه** list comprehension — بتبقى ٣٠٠ استدعاء.
- **متغيّر محلي بيحجب بارامتر** — `names = {}` جوّه `_inv_out` حجبت البارامتر `names`
  وكل فاتورة فيها كوبونات كانت بترجّع 500.

### الفرونت

- **الراوتر HashRouter** — الرابط لازم يكون `https://host/#/path?query`.
  `https://host/path` بيوديك للرئيسية.
- `useListFilter(rows, { search })` — الـ`search` لازم ترجّع **array** مش string.
  وبترجّع `{query,setQuery,values,setValue,range,setRange,reset,filtered,active}` —
  **مافيش `total`**، فمرّر `rows.length` بنفسك.
- `DateRangeFilter` بياخد `value: [Dayjs,Dayjs]|null` و`onChange` — مش `from`/`to`.
- الشاشة الجديدة لازم تتسجّل في **٣ ملفات**:
  `components/PageRoutes.tsx` (الراوت) + `components/TabsContext.tsx` (عنوان التبويب) +
  `components/navigation.ts` (القايمة).
- `npm run build` بيشغّل electron-builder كمان — استخدم `npx vite build` لوحدها.

### دروس عامة

- **متقولش «أخطاء مؤقتة» من غير ما تقيس.** المستخدم رد: «لا مش أخطاء مؤقته دي أخطاء
  موجوده» — والقياس أثبت إنه صح.
- **متخترعش قيم مجال.** «متأكد إن التصنيف ده مطابق لـa5؟» — وماكانش. اتشال.

---

## ٧. ملفات الشات والسجلات

- سجل الجلسة الكامل:
  `C:\Users\Ibrahim Elmahy\.claude\projects\D--techno\33e9020a-d7c0-44e8-b2a4-f33ebad6edc9.jsonl`
- مجلد الشغل المؤقت (السكربتات المساعدة + ملفات SQL + التصديرات):
  `C:\Users\IBRAHI~1\AppData\Local\Temp\claude\D--techno\33e9020a-d7c0-44e8-b2a4-f33ebad6edc9\scratchpad`
- التصديرات على السيرفر: `C:\pgtmp\` و`C:\pgtmp\erp\`
