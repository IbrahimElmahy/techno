/**
 * حجم الصفحة في الجداول — **رقم واحد لكل الشاشات**.
 *
 * كان مكتوب بالإيد في ٨٩ جدول بستة قيم مختلفة (٨، ١٠، ١٢، ١٥، ٢٠، ٢٥، ٥٠) —
 * والفرق مالوش سبب، هو بس اللي كان في دماغ اللي كتب الشاشة ساعتها. فاللي بيشتغل
 * بيلاقي كل شاشة بتعرض عدد مختلف، وبيغيّر الرقم من تحت كل جدول لوحده في كل مرة.
 *
 * ولو اتغيّر تاني، بيتغيّر هنا مرة واحدة.
 */

/** عدد الصفوف الافتراضي في أي جدول. */
export const PAGE_SIZE = 100;

/** الاختيارات اللي بتظهر في «/ الصفحة». */
export const PAGE_SIZE_OPTIONS = ['25', '50', '100', '200', '500'];
