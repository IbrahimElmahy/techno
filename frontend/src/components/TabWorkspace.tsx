import React from 'react';
import PageRoutes from './PageRoutes';
import { TabActiveContext } from './keyboard';

/**
 * مساحة الشغل — شاشة واحدة، زي أودو.
 *
 * كانت تبويبات على شكل كروم: كل قسم بيفتح تبويب، والتبويبات كلها بتفضل مركّبة عشان
 * الفاتورة اللي نصّها مكتوب تفضل مكانها لما ترجعلها. اتشالت بطلب صاحب النظام: القايمة
 * بقت فوق، وشريط تبويبات تحتها كان بيحوّل الصف العلوي لتلات حاجات بتتزاحم على نفس
 * المساحة.
 *
 * **اللي اتفقد معاها:** الشاشة اللي بتسيبها مابتفضلش مركّبة. تفتح فاتورة نصّها مكتوب،
 * تروح لشاشة تانية وترجع — بتلاقيها من الأول. ده تمن التبسيط وهو مقصود، والمستندات
 * بتتحفظ بالحفظ مش بالتبويب.
 *
 * الشاشة المعروضة هي الوحيدة، فهي صاحبة الكيبورد دايماً.
 */
export default function TabWorkspace() {
  return (
    <TabActiveContext.Provider value>
      <PageRoutes />
    </TabActiveContext.Provider>
  );
}
