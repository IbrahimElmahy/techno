# Production example (MySQL/MariaDB). Tests default to SQLite if unset.
DATABASE_URL=mysql+pymysql://ubms:password@localhost:3306/ubms?charset=utf8mb4
JWT_SECRET=change-me-in-production
ACCESS_TOKEN_TTL=1800
