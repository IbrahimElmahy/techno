# برومبت البداية — مشروع techno

انسخ اللي تحت ده كأول رسالة في الشات الجديد.

---

انت شغال على `D:\techno` — نظام ERP بالعربي المصري لشركة تكنو ثيرم. النظام **كله للاختبار
لسه، مفيش أي حاجة بردكشن**.

## البنية

| | |
|---|---|
| `backend/` | FastAPI + SQLAlchemy 2 + Pydantic v2، بايثون 3.11 في `backend/.venv` |
| `frontend/` | React 18 + antd v5 + TypeScript، عربي RTL |
| `mobile/` | **Flutter** (مش React Native) — تطبيق المندوبين، offline بـsqflite |
| قاعدة البيانات | MySQL محلي (`backend/.env`)، والسيرفر Postgres |

**النشر أوتوماتيكي من الجيت هاب** — `git push origin main` وخلاص. الباك إند على Render
والفرونت على Vercel.

- API: <https://api.technothermeg.com> — `/health` بيرجّع الـcommit اللي شغال
- الواجهة: <https://app.technothermeg.com>
- **الرفع بحساب `IbrahimElmahy`** في `gh` (أي حساب تاني بياخد 403)

## قواعد شغل لازم تتبعها

1. **متشغّلش سويتات اختبار.** لا `pytest` ولا `vitest`. الاختبارات اتمسحت كلها بطلب
   المستخدم. `tsc --noEmit` للفرونت (ثواني)، و`python -c "import src.main"` للباك إند،
   و`dart analyze lib/` للتطبيق. لو محتاج تتأكد من سلوك، اكتب سكربت صغير مستهدف وشغّله
   لوحده وامسحه.

2. **متكتبش اختبارات جديدة** — موديل تاني بيعمل دي.

3. **الكلام قليل.** المستخدم زهق من الردود الطويلة. النتيجة الأول، وبعدين اللي يهم بس.

4. **الرفع لما يطلب بس.** اعمل commit محلي وخلاص لحد ما يقول «ارفع».

5. **عمود جديد في قاعدة البيانات؟** لازم يتسجّل في `_ADDED_COLUMNS` في
   `backend/src/main.py` — مافيش alembic. و`ENUM` بيتوسّع بيتحط في `_WIDENED_COLUMNS`.
   من غير كده هيشتغل لوكال ويقع على السيرفر **في صمت** (الفشل بيتبلع عند مستوى info).

6. **متستخدمش `git stash`** خالص.

7. **الباسوردات** — المستخدم بيكتبها بنفسه. أي سكربت محتاج بيانات دخول بيقراها من متغيرات
   البيئة.

## قرارات معمارية مهمة

- **مافيش «عكس» في النظام.** اتشال بالكامل بطلب المستخدم. التعديل بيشيل أثر المستند
  (حركة مخزون، قيد، نقاط) ويعيد بناءه **بنفس الرقم ونفس الـid** — شوف
  `backend/src/services/document_edit_service.py`. والحذف بيشيل الأثر كمان.
  الشرط الوحيد اللي فاضل: فاتورة عليها مرتجع حقيقي مابتتعدّلش لحد ما المرتجع يتمسح.

- **قواعد دفتر الأستاذ الصارمة اتشالت** بطلب المستخدم: إقفال الفترة، قفل التعديل بعد N
  يوم، «القيد لازم سطرين»، «مدين = دائن»، «الترحيل على الحسابات الفرعية بس»، وحراس عدم
  التعديل على القيد وحركة المخزون والنقاط. **الشاشات المحاسبية كلها في مكانها** — شجرة
  الحسابات، كشف الحساب، دفتر الأستاذ، الميزانية. اللي اتشال هو القواعد مش الشاشات.

- **الرصيد مشتق من الحركات**، مش مخزّن. فحذف الحركة بيرجّع الرصيد لوحده.

- **المستندات بتتفتح للعرض دايماً** من أي مكان (`?doc=`)، والتعديل بضغطة «تعديل» من شريط
  الأدوات. `?edit=` لسه شغّال بس `DocumentLink` مابيبعتوش.

- **نظام a5** هو المرجع للمقارنة: <https://a5system-eg.com/> (المستخدم بيفتحه ويسيبه
  مفتوح لما تحتاج تبص). البحث القديم في `specs/029-a5web-parity/`.

- **مافيش باركود** — اتشال بطلب العميل، متضيفهوش تاني حتى لو a5 عنده.

- **المعاينات نقاط بس** — `item_id` بيفضل `null`، ومابتخصمش من عهدة المندوب.

## حاجات تعرفها عن الكود

- `CurrentUser.id` مش `.user_id`
- `/api/v1/stock/on-hand` بيرجّع `{"on_hand": ...}`
- ترتيب الخصم: **سطر ← عميل ← صنف**، وخصم الصنف على شريحة السعر (`item_price.discount_pct`)
  مش على الصنف
- مكان بضاعة المندوب: `rep_store_service.rep_store()` — المخزن الأول وبعده العهدة
- `frontend/src/components/EntryGrid.tsx` بيدير أعمدة شبكات إدخال السطور (إخفاء + ترتيب)
- كاش الطلبات في `frontend/src/api/client.ts` بيطابق **المسار بالظبط** مش `startsWith` —
  عشان كشوف الحساب وكروت الأصناف تفضل حيّة
- فخ متكرر: `TabModal` جوّه فرع القايمة مابيظهرش وشاشة الإنشاء مفتوحة (فيه تعليق بيحذّر
  منه في `Returns.tsx`)
- فخ تاني: `setState` مش شايفها في نفس اللفّة — مرّر القيمة صريحة للدالة

## طريقة الشغل الحالية

المستخدم بيشتغل بـ**Gemini** على التنفيذ، **وانت بتراجع وراه**. لما يقول «راجع»:

1. `tsc --noEmit` + import-check + `dart analyze` — هل بيبني أصلاً
2. دوّر على الأعطال الحقيقية: أزرار بتقول حاجة وبتعمل حاجة تانية، كاش بيلقط داتا حيّة،
   حراس بيجمّدوا أرقام، مفاتيح أجنبية هتقع، تعليقات بتوصف سلوك اتشال
3. **جرّب اللي انت شاكك فيه** — سكربت صغير أحسن من التخمين
4. رجّع بسطرين: إيه المكسور وفين. مش شرح لكل حاجة عملها

آخر commit: `b8d4c4d`. فيه شغل غير مرفوع في الشجرة من Gemini — راجعه قبل أي رفع.
